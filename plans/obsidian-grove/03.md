# 3. Akcje zbiorcze i lista tagów

## Status

Ukończono.

## Zakres

Panel 360 px od 1000 px, poniżej pod galerią. Natychmiastowe tagi, osobny zapis grup, rzeczywiste popularne tagi.

## Źródła w kodzie

src/components/bulk/BulkActionsSidebar.tsx; src/components/tag-list

## Scenariusze odbioru

Single/multi; mieszane ulubione; dane poza cache; retry i częściowe wyniki; include/exclude i paginacja.

## Wykonane sprawdzenia

Desktop UI sprawdził panel 360 px w 1000/1440 px i panel pod galerią w 900 px oraz pełną listę tagów. Testy BulkActionsSidebar i kontrolera obejmują single/multi, brak usuwania tagów dla wielu elementów, popularne tagi z backendu, loading/retry, mieszane ulubione i blokady zapisów. Desktop review-fixes sprawdza literalne tagi, include/exclude i zastosowanie zapytania.

Desktop UI: `artifacts/obsidian-grove/ui-accepted.log`, 9/9 PASS. Dokładne wyniki testów kontrolerów i pozostałych pakietów zapisuje indeks.

## Dowody wizualne

W katalogu UI: `*-bulk.png`, `*-tags.png` i `light-900-stacked-bulk.png`.

## Następny krok

Brak otwartych prac tego etapu. Wyniki i granice odbioru znajdują się w indeksie.
