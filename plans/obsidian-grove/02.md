# 2. Nagłówek, wyszukiwanie i galeria

## Status

Ukończono.

## Zakres

Dwa rzędy 52/44 px, wszystkie filtry i akcje, Ctrl/Cmd+K tylko fokus, wspólny odstęp 12 px.

## Źródła w kodzie

src/components/search; src/components/topbar; src/components/gallery

## Scenariusze odbioru

Draft i zatwierdzone zapytanie; podpowiedzi; modyfikatory i prostokąt; resize; szybki scroll.

## Wykonane sprawdzenia

Desktop UI sprawdził draft bez wysłania przez Ctrl+K, fokus w bulk i blokadę skrótu w modalu, oba motywy, PL/EN, filtry, podpowiedzi, brak wyników i błąd składni. Testy komponentu obejmują również Cmd+K. Desktop selection sprawdził kliknięcia, Ctrl, prostokąt i autoscroll przez 2051 ID. Porównanie TanStack Virtual opisuje indeks planu.

Desktop UI: `artifacts/obsidian-grove/ui-accepted.log`, 9/9 PASS. Dokładne wyniki testów kontrolerów i pozostałych pakietów zapisuje indeks.

## Dowody wizualne

W tym samym katalogu UI: `*-gallery.png`, `*-suggestions.png`, `*-empty.png`, `*-invalid-search.png` oraz `pl-*-gallery.png`.

## Następny krok

Brak otwartych prac tego etapu. Wyniki i granice odbioru znajdują się w indeksie.
